# @package _global_

defaults:
  - _self_
  - data: ba_large # choose datamodule with `test_dataloader()` for evaluation
  - model: gcon
  - logger: wandb
  - trainer: gpu
  - paths: default
  - extras: default
  - hydra: default

  - task: multitask
  - callbacks: default
  - experiment: null

task_name: "eval"

tags: ["dev"]

# passing checkpoint path is necessary for evaluation
ckpt_path: null #???
