defaults:
  - base

net:
  conv:
    _target_: src.models.layer.general.LayerWrapper
    layer:
      _target_: torch_geometric.nn.conv.GINConv
      nn:
        _target_: torch.nn.Sequential
        _args_:
        - _target_: torch.nn.Linear
          in_features: ${model.net.dim_inner}
          out_features: ${model.net.dim_inner}  # or whatever hidden dimension you want
        - _target_: torch.nn.ReLU
        - _target_: torch.nn.Linear
          in_features: ${model.net.dim_inner}
          out_features: ${model.net.dim_inner}
      train_eps: false  # optional: whether to learn the epsilon parameter
