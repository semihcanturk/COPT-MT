# @package _global_

# to execute this experiment run:
# python src/train.py experiment=gcn_node_classification

defaults:
  - override /data: gnn_benchmark.yaml
  - override /model: gcn.yaml
  - override /callbacks: default.yaml
  - override /trainer: default.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["graph", "gcn", "gnn_benchmark", "node_classification"]

seed: 12345

trainer:
  min_epochs: 10
  max_epochs: 100
  gradient_clip_val: 0.5

model:
  task: "node"
  num_classes: 6  # For CLUSTER dataset
  net:
    in_channels: 7  # Number of node features in CLUSTER dataset
    out_channels: 6  # Should match num_classes
    task: "node"

data:
  dataset_name: "CLUSTER"
  batch_size: 32

logger:
  wandb:
    tags: ${tags}
    group: "gcn_node_classification"