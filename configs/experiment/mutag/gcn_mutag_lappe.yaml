# @package _global_

# to execute this experiment run:
# python src/train.py experiment=csl/gcn_csl_lappe

defaults:
  - mutag/gcn_mutag.yaml

# all parameters below will be merged with parameters from default configurations set above
# this allows you to overwrite only specified parameters

tags: ["graph", "gcn", "mutag", "graph_classification", "lappe"]

model:
  net:
    in_channels: 12  # Number of original (7) + LapPE (5) features

data:
  transforms:
    laplacian_pe:
      _target_: torch_geometric.transforms.AddLaplacianEigenvectorPE
      k: 5  # Number of Laplacian eigenvectors to use
      is_undirected: True
      attr_name: null

logger:
  wandb:
    tags: ${tags}
    group: "gcn_graph_classification"
